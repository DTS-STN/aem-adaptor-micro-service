[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
uvicorn = "*"
fastapi = "*"

[dev-packages]
requests = "*"
pytest = "*"

[requires]
python_version = "3.9"

[scripts]
test = "pipenv run pytest"
app = 'pipenv run uvicorn app.main:app --host "0.0.0.0" --port 8000 --reload'